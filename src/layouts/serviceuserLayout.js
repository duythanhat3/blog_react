import React from 'react';
let serviceuserLayout = ({children}) => (
    <div>
        <div className="all-wrapper ">
            {children}
        </div>
    </div>
);
export default serviceuserLayout;