import React from 'react';
let servicelogsLayout = ({children}) => (
    <div>
        <div className="all-wrapper with-pattern">
            {children}
        </div>
    </div>
);
export default servicelogsLayout;